---
import TranslationMenu from "../components/TranslationMenu.astro";
import BaseLayout from "./BaseLayout.astro";
import { t } from "astro-i18n";

export interface Props {
  title: string;
}

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <header class="bg-stone-700 text-stone-50">
    <div class="container mx-auto p-4">
      <a
        href="#main"
        class="transition left-0 top-0 absolute p-3 m-3 -translate-y-16 focus:translate-y-0 bg-stone-700 text-stone-50"
      >
        {t("header.skip")}
      </a>
      <h1 class="font-bold text-6xl">{t("header.name")}</h1>
      <TranslationMenu />
    </div>
  </header>
  <hr class="hidden" />
  <main id="main" class="container mx-auto p-4 flex-1 space-y-4">
    <slot />
  </main>
  <footer class="bg-stone-700 text-stone-50 p-4 text-center">
    <p>{t("footer.content")}</p>
  </footer>
</BaseLayout>
